# docs

Place a screenshot of the module (as it appears on the mirror) here as
`screenshot.png` — the top-level README references `docs/screenshot.png`.

Guidance (per the MagicMirror module docs): use a clear, high-quality image of the
module running against a black background, cropped to the module. PNG preferred.
